Relational Databases
* A relationable database stores and orginazes data in a structured format using tables

Schema
 In the context of databases, a schema os a blueprint or achitecture that defines the strictire of a database.
 it outlines and provides constraints on how data is organized and how the relationships between dofferent tables and fields structured.

 A schema essentially serves as a map or framework that guides how data is stored, accessed, and manipulated.

Conceps
 * A table is acollection of related data entries and consists of rows and colims. Each table in a data base represents a different
 entity, like customers, ordes, products, etc.

 Colomns represent the arrtibutes or firelds of the entity.

Joins
 In relational databases, joins are used to combine rows and frotm two or more tables vvased on a related column between them.

 A primary key is a unique identifier for each record in a table.

 A foreign ket is a field (or a collection of fields) in one table that uniquely identifies a row of another table. the foreign
 key establishes a link between the two tables.

SQL (Structured Query Language)
 Is the standard language used to interact with relational databases. it allows you to create, read, update, and delete dat in the database

Advantages
 Data Integrity.
 Flexibility.
 Scalability.
